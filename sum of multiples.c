#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("I calculate the sum of unique multiples of particular numbers up to a maximum (not included) N\n");
    printf("Give me N\n");
    int maximum;
    scanf("%d", &maximum);
    printf("How many numbers have you got?\n");
    int num_of_divisors;
    scanf("%d", &num_of_divisors);
    for(int i = 0; i )

    /*int number;
    int i = 1;
    int counter = 0;
    while (i <= number) {
        if(number % i == 0) {
            printf("%d ", i);
            ++counter;
        }
        ++i;
    }
    This is the code to find out if a number is prime*/
    return 0;
}
